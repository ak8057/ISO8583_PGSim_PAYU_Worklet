# Tests: avoid clashing with a dev instance on BRD defaults (8080/8081).
server.port=0
simulator.tcp.port=19443
simulator.timeout=30000
pgsim.connection.timeout=30000
pgsim.iso.secondary-packager=
pgsim.config.persistence.enabled=false
pgsim.tcp.tls.enabled=false
simulator.tcp.enabled=false

management.endpoints.web.exposure.include=health,metrics,info,prometheus
management.prometheus.metrics.export.enabled=true
